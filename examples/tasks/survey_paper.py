"""
任务配置：综述论文——大型语言模型在软件工程中的应用

目标：
    生成一篇结构完整的学术综述，系统梳理大型语言模型在软件工程中的主要应用场景、
    代表性方法、现实挑战与未来研究方向，并体现长文本生成中的规划、连贯性维持与
    自我修正能力。

设计原则：
    1. 将“至少 5 篇研究引用”“至少两类方法比较”“未来方向讨论”视为全文级目标，
       不再作为每节在线硬约束。
    2. 全局约束只保留适合逐节判断的风格、连贯性和篇幅要求。
    3. 通过 outline 显式指定哪些章节承担方法比较、案例分析、未来方向等职责。
"""
from typing import Dict, List


def get_task_config() -> Dict:
    """
    返回综述论文任务配置

    返回：
        {
            'task':         任务描述字符串
            'constraints':  全局约束列表（适合逐节在线判断）
            'outline':      章节大纲 {section_id: section_title}
            'session_name': 会话名称（用于文件命名）
        }
    """
    task: str = (
        "写一篇关于大型语言模型在软件工程中的应用的综述论文。"
        "全文应系统梳理该领域的研究背景、技术基础、代表性方法、实际应用案例、主要挑战与未来方向。"
        "全文需要至少提及 5 项相关研究工作或代表性论文，并在方法比较章节中明确比较至少两类大型语言模型"
        "或两种技术路线在软件工程任务中的表现差异。结尾部分需要总结全文，并讨论未来研究方向。"
    )

    constraints: List[str] = [
        "语言保持客观、学术、克制，避免口语化和主观夸张评价",
        "各节内容需与本节标题职责一致，不得大篇幅提前展开后续章节内容",
        "全文长度控制在约 1500 字左右，允许合理浮动",
    ]

    outline: Dict[str, str] = {
        "intro":        "引言：研究背景、问题重要性与综述范围",
        "background":   "背景：LLM 技术基础与软件工程任务类型",
        "methods":      "方法比较：不同 LLM 或技术路线在软件工程任务中的表现差异",
        "case_studies": "案例研究：代码生成、缺陷检测、测试等代表性应用实例",
        "challenges":   "挑战与局限性：可靠性、安全性、评测与工程落地问题",
        "future":       "未来方向：潜在改进路径与值得关注的研究议题",
        "conclusion":   "结论：总结全文主要发现并呼应未来方向",
    }

    return {
        "task":         task,
        "constraints":  constraints,
        "outline":      outline,
        "session_name": "survey_paper",
    }